%{
#include <stdio.h>
#include "y.tab.h"   /* generated by yacc/bison */
%}

%%
[0-9]+      { yylval = atoi(yytext); return NUMBER; }
[+\-*/%]    { return yytext[0]; }
\n          { return '\n'; }
[ \t]       { /* ignore whitespace */ }
.           { return yytext[0]; }
%%

int yywrap() {
    return 1;
}
